# Build & Cargo
target/
bin/ynab-cli

# backend/Cargo.lock is intentionally committed: this crate ships a binary,
# so the lockfile is what makes builds reproducible and auditable.

# Secrets & Environment
.token
*.token
*.key
*.pem
.env
.env.*
!.env.example

# Cache & Local State
.cache/
*.log
*.tmp
